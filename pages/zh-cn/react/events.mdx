---
title: 事件响应
---

import nativeEvent from '../../../examples/files/react/nativeEvent.js'
import customEvent from '../../../examples/files/react/customEvent.js'

一些组件会响应用户行为，并触发回调事件。事件处理逻辑函数可以过 Prop 属性的方式传入至 React 元素：

<Example
  code={nativeEvent}
  console={{
    enabled: true,
    visible: true,
  }}
  playground={{ enabled: false }}
/>

---

## 自定义事件

假设我们要自定义一个包含 `onIncrement` 回调函数的 `CounterButton` 组件，<Details>我们可以把这个回调函数传入到按键元素的 `onPress` 属性。</Details>

<Example code={customEvent} />
